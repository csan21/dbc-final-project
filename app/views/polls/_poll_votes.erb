<% poll.answers.each do |answer| %>
  <%= answer.text %> - <%= answer.votes.count %> votes
  <% if poll.votes.count > 0 %>
    (<%= ((answer.votes.count.to_f / poll.votes.count)*100).floor %>%)
  <% end %>
  <br>
<% end %>
<br>

<%= pie_chart poll.answers.group(:text).count %>
